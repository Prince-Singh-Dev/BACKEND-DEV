<!--Basically we are going to craete a UI for create user interface ...... and for that we need to go through from these various steps ......
            1. mongoose setup
            2. schema 
            3. model setup
            4. creating a user -> password -> hash
            5. jwt Token -> cookie


Furthur Login part .......... Login -> token -> decrypt -> email 
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="min-h-screen bg-zinc-900 flex items-center justify-center px-4">
        <form method="post" action="/create" class="w-full max-w-md bg-zinc-800 p-6 rounded-xl shadow-lg space-y-5">
            <h2 class="text-2xl font-semibold text-white text-center mb-4">Create a New User</h2>
            <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:border-white transition" type="text" placeholder="Username" name="username" >
            <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:border-white transition" type="email" placeholder="Email" name="email" >
            <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:border-white transition" type="password" placeholder="Password" name="password" >
            <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:border-white transition" type="number" placeholder="Age" name="age" >
            <input class="w-full px-4 py-3 rounded-md bg-white text-zinc-900 font-semibold hover:bg-zinc-300 cursor-pointer transition" type="submit" value="Create User">
            </form>
    </div>

</body>
</html>